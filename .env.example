# club — docker-compose 双环境版本管理
# 每个 service 从 .env 读取自己要跑的镜像 tag（对应 package.json version）。
#
# 初始值（首次部署前必须手动填上具体版本，例如 0.1.0）：
#   CLUB_TEST_TAG=
#   CLUB_PROD_TAG=

# 预发布 / staging 镜像版本
CLUB_TEST_TAG=
# 生产镜像版本
CLUB_PROD_TAG=
