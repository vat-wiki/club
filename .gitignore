node_modules/
dist/
*.db
*.db-shm
*.db-wal
.DS_Store
# Claude Code 调度状态（每小时变动，不入库）
.claude/scheduled_tasks.json
.claude/scheduled_tasks.lock
# TypeScript 增量构建缓存（每次 build 变动，不入库）
*.tsbuildinfo
# Playwright 自动化运行产物（时间戳日志/快照）
.playwright-cli/