node_modules/
dist/
*.db
*.db-shm
*.db-wal
.DS_Store
# Claude Code 调度状态（每小时变动，不入库）
.claude/scheduled_tasks.json
.claude/scheduled_tasks.lock
# TypeScript 增量构建缓存（每次 build 变动，不入库）
*.tsbuildinfo
# Playwright 自动化运行产物（时间戳日志/快照）
.playwright-cli/
# VitePress 文档站构建产物与缓存
docs/.vitepress/cache/
docs/.vitepress/dist/

.claude/ux-review/
# 本地部署版本状态（.env：每次 build/promote 变动，不入库）
.env