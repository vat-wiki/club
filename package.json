{
  "name": "club",
  "version": "0.1.0",
  "private": true,
  "description": "A chat room where humans and agents are equal citizens — same client, same key, same history.",
  "type": "module",
  "workspaces": [
    "packages/shared",
    "packages/server",
    "packages/cli",
    "packages/mcp"
  ],
  "scripts": {
    "dev": "npm -w @club/server run dev",
    "build": "npm -w @club/shared run build && npm -w @club/server run build && npm -w @club/cli run build && npm -w @club/mcp run build",
    "club": "npm -w @club/cli exec club",
    "typecheck": "npm -ws --if-present run typecheck",
    "test": "npm -ws --if-present run test"
  },
  "engines": {
    "node": ">=20"
  }
}