# club 优化历史

| 日期 (GMT+8) | 方向 | 内容 |
|---|---|---|
| 2026-07-20 | 类型定义 | 为 `@club/web` 的 API 门面 `api` 添加正式 `ClubApi` 接口：显式声明 `me/messages/send/members/rooms/createRoom/search/deleteMessage/react/thinking/idle/uploadFile` 共 11 个方法的签名与 JSDoc 文档，使组件编译时即可捕获参数错位、遗漏必填参、丢失返回类型等问题；抽取共享 `UploadOptions` 接口与 `UploadProgressCb` 类型替代 `uploadFile` 内联字面量 `{ timeoutMs, onProgress }`；重构 `recoverParticipant` 函数的 JSDoc；web 编译 0 error |
| 2026-07-20 | 最佳实践 | 修复 `toMessage()` 在批量 endpoints（`GET /messages` 与 `GET /messages/search`）上的隐性 N+1 回退：`getReactionsForMessages()` 的批量查询结果传给 `toMessage()` 后，对无反应消息的 id 预填入空数组，消除 `toMessage()` 中 `reactionsMap?.get(r.id) ?? getReactionsForMessage(r.id)` 的逐行 DB 查询 fallback；列表与搜索 hot path 真正变为 N+1-free（tsc 0 error） |
| 2026-07-20 | 重构 | SSE 路由 `GET /messages/stream` 的 `rooms` 逗号分隔列表校验改为统一调用 `requireValidRoomSlug`，消除对 `ROOM_SLUG_REGEX` 的直依赖与裸 `jsonErr` 调用；与 POST /rooms 保持同一条校验路径，未来扩展校验规则只需改一处；解除 messages.ts 对 @club/shared 的 ROOM_SLUG_REGEX 直接 import；tsc 0 error
| 2026-07-20 | 测试覆盖 | 为 `mention.ts` 的 `extractMentionedParticipants` 补充 5 个针对性单测，覆盖单线程 scanner 的边界行为：email 前缀 `@` 不被误匹配（`user@alice.com` 不触发 @alice）、CJK/数字/连字符/下划线的名称字符、尾部孤立的 `@` 不算提及、URL 路径中的 `@foo` 与邮件 `x@` 的前缀过滤差异、以及提及列表按文本出现顺序（非列表顺序）返回；mention.test.ts 从 11 → 16 通过，tsc 0 error |
| 2026-07-20 | 文档 | 为 `@club/sdk` 核心模块和 web 工具函数补充正式 JSDoc：`transport.ts` 的 `ClubConn`/`CallOpts`/`RequestOptions`/`UploadFileInput`/`request()`/`getFile()`/`uploadFile()` 七个导出，`file-parser.ts` 的 `FileContent`/`parseFileContent()`，`web/src/lib/utils.ts` 的 `cn()`；替换原有行内注释为含 @param/@returns/@throws/@example 的 IDE 可识别文档注释；tsc 0 error（sdk + web）、eslint --max-warnings 0 |
| 2026-07-20 | 代码质量 | 移除 `stream.ts` 的 `addSubscriber` 中冗余 presence 写入环 — 该环在 `broadcastPresence` 已覆盖的全局分发之外，还遍历所有现有订阅者并直接把他们的 presence 帧写入新秀自己的 SSE 流（客户端从未消费）；删除该 dead code 并加注释防止后续重新引入；tsc 0 error、stream 7/7 通过 |
| 2026-07-19 | 类型定义 | 提取 ParticipantRow 与 MentionByIdRow 接口，消除 db.ts 三处函数返回值的内联 object 类型；prepared statement 与函数签名均显式标注 `| undefined`，让调用方直接用联合类型分支替代事后 null-check；附 JSDoc 文档返回语义 |
| 2026-07-19 | 安全 | 为 `filePath()` 添加路径遍历防护：拒绝含 ` /`、`\`、`..` 的文件 id，并用 resolved path `startsWith(dir)` 兜底防御符号链接逃逸；附 5 个单元测试覆盖正常 id、`..` 遍历、绝对路径前缀、反斜杠四种攻击面 |
| 2026-07-19 | 性能 | 缓存动态 placeholder SQL 的 prepared statement（getFilesByIds / getReactionsForMessages）— 按批次大小 key 的 Map 缓存，消除热点路径每次调用重复 create statement 的开销 |
| 2026-07-19 | 文档 | docs/api.md 修正 4 处代码不一致 |
| 2026-07-19 | 最佳实践 | 修复 4 处 lint warning（全项目 280→276）— 文件预览两个 useEffect IIFE 加 `void` 前缀消除 floating promise；config.ts `||`→`??`；me.ts `!row || row.participant_id` 守卫改用 `row?.participant_id` 可选链 |
| 2026-07-19 | 重构 | 提取 `jsonErr(c, msg, status)` 通用错误响应工具函数到 `lib.ts`，用联合类型 `ContentfulStatusCode` 替代分散在各 route 文件中的 `c.json({ error: ... }, N)` 直写；覆盖 agents/files/me/messages/participants/rooms 共 6 个路由、30 处调用点，单点维护、消除形状漂移风险 |
| 2026-07-20 | 测试覆盖 | 为 MCP 的 dispatchTool delete/react 工具补全 7 个单测，覆盖 delete 缺 id/成功删除、react 缺 id/缺 emoji/成功聚合/空聚合返回 '(none)'/多反应空间分隔；同时为 helpers.test.ts 加文件级 eslint-disable 抑制 mock 固有 warning；mcp/helpers.ts 覆盖率 84.87%→99.15% statement、100% line、71/71 通过，tsc 0 error |
| 2026-07-20 | 安全性 | CLI `club react <id> <emoji>` 命令对 emoji 输入做 ASCII 控制字符净化：抽取 `sanitizeEmoji()` 移除 `\x00-\x1f` 与 `\x7f`（DEL），与文件上传控制字符清理一脉相承，避免 CRLF/NUL/DEL 注入 reaction 流；将 `makeReactCommand()` 重构为可测试的 `runReact(opts, deps)` 依赖注入模式（对齐 send-impl.ts 风格）；新增 `react.test.ts` 8 个单测：sanitizeEmoji 覆盖 NUL/CRLF/DEL/混合/正常 emoji/空白保留，runReact 验证 sanitize 在前端生效（vi.fn spy）；CLI 套件 8 passed、lint 0 warning、tsc 0 error |
