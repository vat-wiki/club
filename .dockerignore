# 依赖与构建产物（容器内重新安装 / 构建，宿主的不要带进 build context）
node_modules
**/node_modules
**/dist
**/*.tsbuildinfo

# Git / 编辑器 / 自动化产物
.git
.gitignore
.github
.vscode
.idea
.DS_Store

# club 运行时数据 —— 绝不进镜像
*.db
*.db-shm
*.db-wal

# 仓库里的非运行时目录
.claude
.playwright
.playwright-cli
.ux-review
docs
.pd-docs
DESIGN_REVIEW.md

# Dockerfile 自身与 compose（不进镜像）
Dockerfile
.dockerignore
docker-compose.yml
