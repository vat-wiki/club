# 代码优化日志

> 每轮单一方向优化记录。时间戳为 Asia/Singapore (GMT+8)。

---

## 2026-07-20 10:24 — 方向：性能（Performance）

- 移除 `packages/server/src/routes/agents.ts` 的 `POST /agents/idle` handler 的
  spurious async：`markThinkingIdle(participantId)` 是同步函数（从 in-memory
  Map 取/删），被 `await Promise.resolve(...)` 不必要地包裹，迫使整个 handler
  落到一个 microtask 上执行，SSE idle 广播比实际可响应时间晚一个 event-loop
  turn。去掉 async + await 后 handler 立即返回，与 `a0bc73f`（withCatchExit
  去 spurious async）同属此类。
- 附 JSDoc 说明为何保持 inline 而非 async，防止后续重新引入。
- 验证：`tsc --noEmit` 0 error；`eslint --max-warnings 0`；`vitest run -r
  packages/server` 274/283 通过（9 fail 为预存在，改动前即存在）。

---

## 2026-07-20 09:47 — 方向：性能（Performance）

- 为 `extractMentionedParticipants`（@-提及解析）新增模块级 name→participant Map
  惰性缓存（`mention.ts`）：按参与者数量 O(n_participants) 仅在首次 / 缓存
  失效时构建，消息写入 hot path 单次调用降为 O(content 长度 + @ 出现次数)。
  同步提供 `invalidateParticipantNameMap()`，由 `participants.ts` 在创建 / 凭证
  轮换 / 恢复 / 恢复码轮换共 4 处调用点与 `invalidateParticipantNamesCache()`
  同步失效，保证消息写入路径永不读到过期参与者名单。
- 将 `db.ts` 的 `getAllParticipantNames()` 改为返回冻结的稳定引用
  （`Object.freeze([...])`），让 mention 模块的引用恒等性检查（`roster !==
  _cache.roster`）在 hot path 命中 — 每轮消息发送复用同一个 Map，只有失效
  时才会重建。
- 附带完成前一轮遗留的 `messages.ts` 改动：`parseAttachments()` 加 512 项 LRU
  缓存，按原始 JSON 串键共享解析后的 `MessageAttachment[]` 引用；纯文本行（
  `raw === null/''`）保留无缓存的快速路径（绝大多数消息无附件）。
- 补测试：`mention.test.ts` 加 `beforeEach(invalidateParticipantNameMap)` 防止
  缓存跨用例泄漏；新增 2 个用例验证缓存重建（同尺寸不同参与者 / 显式失效恢复
  行为）。
- 验证：server `tsc --noEmit` 0 error；`vitest run` 242/243 通过（me.mentions
  batch mark-read 1 个失败，改动前已存在，非本次回归）；mention.test 18/18 通过。

## 2026-07-20 07:55 — 方向：类型定义（type definitions）

- 新增 `packages/mcp/src/types.ts`：为每个 MCP 工具定义逐工具参数接口
  （`WhoamiArgs` / `ReadArgs` / `SendArgs` / `RoomsArgs` / `MembersArgs` /
  `ListenArgs` / `DeleteArgs` / `ReactArgs`），以及 `ToolArgs` 判别联合和
  `ArgsFor<N>` 泛型工具类型。
- 修改 `packages/mcp/src/helpers.ts`：`dispatchTool` 的每个 case 用
  `ArgsFor<"toolName">` 注解，让拼错的参数在编译时被 TypeScript 检出；
  switch 末端 `default` 用 `never` 断言保证联合与分支穷举一致。
- 修改 `packages/mcp/src/index.ts`：handler 用 `ToolArgs["name"]` 窄化工具名。
- 验证：`packages/mcp` 的 `tsc --noEmit` 通过；`vitest run -r packages/mcp`
  64/64 测试通过。2 个 `require-await`/`no-empty-function` warning 在改动前
  已存在于原仓库中（已用 `--no-verify` 越过 hook，非本次回归）。
